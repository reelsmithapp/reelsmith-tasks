# Database Configuration (Neon PostgreSQL)
DATABASE_URL=postgresql://user:password@host.neon.tech/reelsmith_tasks?sslmode=require

# API Configuration
PORT=3000
NODE_ENV=development

# Security (CRITICAL - Keep secret!)
# Generate a secure random API key using: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
# Example: a7k2J8fL3mQ9pR1vX4nW6sY8tZ5bC0dE2fG4hJ6kL8m
API_KEY=your-secure-random-api-key-here

# Frontend Build Configuration
# These are used by Vite during build time
VITE_API_URL=http://localhost:3000
VITE_API_KEY=your-secure-random-api-key-here

# Frontend Configuration (for CORS)
# In production, set to your actual domain or * for public API
FRONTEND_URL=*

# Production Configuration (for Railway deployment)
# DATABASE_URL will be automatically set by Railway
# Set these in Railway environment variables:
# - API_KEY (generate a secure random key)
# - VITE_API_KEY (same as API_KEY)
# - VITE_API_URL (your Railway app URL, e.g., https://reelsmith-tasks.railway.app)
# - NODE_ENV=production
