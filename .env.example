# Database Configuration (Neon PostgreSQL)
DATABASE_URL=postgresql://user:password@host.neon.tech/reelsmith_tasks?sslmode=require

# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend Configuration (for CORS)
FRONTEND_URL=http://localhost:5173

# Production URLs (for Railway deployment)
# DATABASE_URL will be automatically set by Railway
# FRONTEND_URL=https://reelsmith-tasks.railway.app
